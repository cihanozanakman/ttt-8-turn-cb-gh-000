#!/usr/bin/env ruby
require_relative '../lib/turn'

actionCount = 0
board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]
puts "Welcome to Tic Tac Toe!"
display_board(board)
turn(board)
while actionCount < 10
  
end
value = gets.strip
value = value.to_i
if value < 10 && value > 0 
  
end
